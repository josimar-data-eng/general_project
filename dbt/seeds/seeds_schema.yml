version: 2

seeds:
  - name: employee
    description: Map manual employee
    columns:
      - name: employee_id
        description: Employee identification.
        tests:
          - not_null
          - unique